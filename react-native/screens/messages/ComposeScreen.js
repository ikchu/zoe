import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const ComposeScreen = (props) => {
  return (
    <View>
      <Text>Compose Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({

});

export default ComposeScreen;
